function qiyriq() {
    const pricePerItem = 45000; 
    const quantity = prompt("Сколько хотите?");

    switch (true) {
        
        default:
            const totalPrice = quantity * pricePerItem; // Расчёт общей стоимости
            alert(`Общая стоимость: ${totalPrice} сум`);
    }
}
